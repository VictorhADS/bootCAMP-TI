# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.


          # Trecho essencial do ci.yml para gerar artefatos:
# ...
- name: Testes E2E
  run: npm run test:e2e || npm run test:e2e -- --reporter=html
- name: Publicar relatório do Playwright
  if: always()
  uses: actions/upload-artifact@v4
  with:
    name: playwright-report
    path: playwright-report
- name: Publicar pacote da extensão
  uses: actions/upload-artifact@v4
  with:
    name: extension-zip
    path: dist/extension.zip
# ...
name: CI # Sem indentação

on: # Sem indentação
  push: # Dois espaços de indentação
    branches: [ main ] # Quatro espaços de indentação
  pull_request: # Dois espaços de indentação
    branches: [ main ] # Quatro espaços de indentação

jobs: # Sem indentação
  test-build: # Dois espaços de indentação
    runs-on: ubuntu-latest # Quatro espaços de indentação
    steps: # Quatro espaços de indentação
      - uses: actions/checkout@v4 # Seis espaços de indentação
      # ... (Outros passos, sempre alinhados com '- uses' ou '- name')

      # Etapas CRUCIAIS para o Item 3 (Relatório):
      - name: Testes E2E # Seis espaços
        run: npm run test:e2e || npm run test:e2e -- --reporter=html # Oito espaços
      - name: Publicar relatório do Playwright # Seis espaços
        if: always() # Oito espaços
        uses: actions/upload-artifact@v4 # Oito espaços
        with: # Oito espaços
          name: playwright-report # Dez espaços
          path: playwright-report # Dez espaços
      - name: Publicar pacote da extensão # Seis espaços
        uses: actions/upload-artifact@v4 # Oito espaços
        with: # Oito espaços
          name: extension-zip # Dez espaços
          path: dist/extension.zip # Dez espaços
